export interface IConfig {
  x_client_id: string;
  x_client_secret: string;
  x_callback: string;
  x_state: string;
  x_url: string;
  redis_url: string;
  redis_port: number;
  client_url: string;
}
